# GPS Tracker data collector

service tracker
{
	disable         = no
        socket_type     = stream
        protocol        = tcp
        #socket_type     = dgram
        #protocol        = udp
	wait            = no
	user            = root
	server          = /usr/local/sbin/GpsTracker
}
